[pytest]
testpaths = tests
markers =
    integracao: contrato contra backend vivo (precisa PAINEL_API_URL/PAINEL_API_TOKEN)
filterwarnings =
    ignore:The .fitz. API is deprecated:DeprecationWarning
